<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.shopping.dao.CartItemsDao">

    <resultMap type="com.shop.shopping.POJO.entity.CartItems" id="CartItemsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="productId" column="product_id" jdbcType="INTEGER"/>
        <result property="quantity" column="quantity" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.cart_items(user_idproduct_idquantity)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.userId}#{entity.productId}#{entity.quantity})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.cart_items(user_idproduct_idquantity)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}#{entity.productId}#{entity.quantity})
        </foreach>
        on duplicate key update
user_id = values(user_id) product_id = values(product_id) quantity = values(quantity)     </insert>

</mapper>

