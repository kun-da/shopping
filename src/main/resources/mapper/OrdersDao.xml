<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.shopping.dao.OrdersDao">

    <resultMap type="com.shop.shopping.POJO.entity.Orders" id="OrdersMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="totalAmount" column="total_amount" jdbcType="NUMERIC"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="createdAt" column="created_at" jdbcType="TIMESTAMP"/>
        <result property="updatedAt" column="updated_at" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.orders(user_idtotal_amountstatuscreated_atupdated_at)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.userId}#{entity.totalAmount}#{entity.status}#{entity.createdAt}#{entity.updatedAt})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.orders(user_idtotal_amountstatuscreated_atupdated_at)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}#{entity.totalAmount}#{entity.status}#{entity.createdAt}#{entity.updatedAt})
        </foreach>
        on duplicate key update
user_id = values(user_id) total_amount = values(total_amount) status = values(status) created_at = values(created_at) updated_at = values(updated_at)     </insert>

</mapper>

