<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.shopping.dao.CategoriesDao">

    <resultMap type="com.shop.shopping.POJO.entity.Categories" id="CategoriesMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="createdAt" column="created_at" jdbcType="TIMESTAMP"/>
        <result property="updatedAt" column="updated_at" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.categories(namedescriptioncreated_atupdated_at)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.name}#{entity.description}#{entity.createdAt}#{entity.updatedAt})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.categories(namedescriptioncreated_atupdated_at)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}#{entity.description}#{entity.createdAt}#{entity.updatedAt})
        </foreach>
        on duplicate key update
name = values(name) description = values(description) created_at = values(created_at) updated_at = values(updated_at)     </insert>

</mapper>

