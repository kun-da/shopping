<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.shopping.dao.CouponsDao">

    <resultMap type="com.shop.shopping.POJO.entity.Coupons" id="CouponsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="code" column="code" jdbcType="VARCHAR"/>
        <result property="discount" column="discount" jdbcType="NUMERIC"/>
        <result property="expiration" column="expiration" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.coupons(codediscountexpiration)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.code}#{entity.discount}#{entity.expiration})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.coupons(codediscountexpiration)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.code}#{entity.discount}#{entity.expiration})
        </foreach>
        on duplicate key update
code = values(code) discount = values(discount) expiration = values(expiration)     </insert>

</mapper>

