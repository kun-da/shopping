<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.shopping.dao.OrderItemsDao">

    <resultMap type="com.shop.shopping.POJO.entity.OrderItems" id="OrderItemsMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="orderId" column="order_id" jdbcType="INTEGER"/>
        <result property="productId" column="product_id" jdbcType="INTEGER"/>
        <result property="quantity" column="quantity" jdbcType="INTEGER"/>
        <result property="price" column="price" jdbcType="NUMERIC"/>
        <result property="totalPrice" column="total_price" jdbcType="NUMERIC"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.order_items(order_idproduct_idquantitypricetotal_price)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.orderId}#{entity.productId}#{entity.quantity}#{entity.price}#{entity.totalPrice})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into shopping.order_items(order_idproduct_idquantitypricetotal_price)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.orderId}#{entity.productId}#{entity.quantity}#{entity.price}#{entity.totalPrice})
        </foreach>
        on duplicate key update
order_id = values(order_id) product_id = values(product_id) quantity = values(quantity) price = values(price) total_price = values(total_price)     </insert>

</mapper>

